<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>G64 Reads: Books</title>
    <link rel="stylesheet" href="/main.css">
  </head>
  <body>
    <header>
      <a href="/">
        <img id="logo" width="110px" height="80px" src="/bookLogo.png" alt="an open book">
        <h1>Galvanize Reads</h1>
      </a>
    </header>
    <div id="bookArea">

      {{#if bookData}}
          <div class="homeBooks">
            <img class="bookCovers" src="{{bookData.url}}" alt="{{bookData.title}}">
            <div class="homeBookInfo">
              <h3>{{bookData.title}}</h3>
              <h5>by {{bookData.authors }}</h5>
              <h5>{{bookData.genre}}</h5>
              <p> {{bookData.desc}} </p>
            </div>
          </div>
      {{/if}}

    </div>
  </body>
</html>
